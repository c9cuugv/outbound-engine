-r requirements.txt

# Enforces the per-test ceiling set in pytest.ini, so a hang fails fast and
# names the culprit instead of stalling the run.
pytest-timeout==2.4.0

# Testing
pytest==8.3.0
pytest-asyncio==0.24.0
aiosqlite==0.22.1
